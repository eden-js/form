<element-phone>
  <span>
    <span if={ get('data.value') }>{ get('data.value') }</span>
    <i if={ !get('data.value') }>N/A</i>
  </span>

  <script>
    // import base
    import Base from '../js/base';

    // export default
    export default class ElementPhone extends Base {}
  </script>
</element-phone>
